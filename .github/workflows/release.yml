name: Release Workflow

on:
  push:
    branches:
      - 'rc/**'
      - 'main'

jobs:
  action:
    uses: AlaskaAirlines/auro-actions/.github/workflows/no-build_release.yml@dev
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}